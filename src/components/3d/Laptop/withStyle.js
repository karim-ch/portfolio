import styled from "styled-components"

export default component => styled(component)`
  .laptop {
    width: 3em;
    height: 2.6em;
    position: absolute;
    bottom: -4.7em;
    left: -1em;
  }
`
