import React from "react"

import withStyle from "./withStyle"

const Projects = ({ className }) => {
  return (
    <section className={className}>
      <h2 className="heading"> My Projects & Awards </h2>
      <div>haa</div>
    </section>
  )
}

export default withStyle(Projects)
